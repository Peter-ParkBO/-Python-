-------------------------------------------------------------------------------------------------

# 변수 : 데이터를 저장하는 공산. 변하는 수

# 변수의 선언 : 변수를 생성하는 것
num = 10        # = 는 대입연산이라고 해서, 우변의 값을 좌변에 대입한다

print("num")
print(num)      # 변수의 호출 : 선언한 변수를 참조하는 것
print()

print("num =", num)

# 변수 = 변하는 수
# 변수는 단일값만 저장 가능하고, 여러번 저장시 마지막 데이터가 남는다
num = 20        # ※ 이미 선언한 변수에 값을 채우면, 값이 변경됨

print("num =", num)

-------------------------------------------------------------------------------------------------

#Python 변수의 자료형은 대입되는 값에 따라서 자동으로 결정된다

n1 = "10"
n2 = 10
n3 = 10.0

print("n1의 타입 :", type(n1))
print("n2의 타입 :", type(n2))
print("n3의 타입 :", type(n3))

# 변수는 아래와 같이 한번에 초기화 가능
num1, num2 = 10, 20

print("num1 =", num1, ", num2 =", num2)

-------------------------------------------------------------------------------------------------

# 변수에 저장된 데이터도 연산가능
n1, n2 = 10, 3.14

print("n1 =", n1, ", n2 =", n2, "\n")

print("n1 + n2 =", n1 + n2, "\n")

# 산술연산은 변수의 값만 가져와서 연산 할 뿐, 변수에 변화를 주지 않음
print("n1 =", n1, ", n2 =", n2, "\n")

# 대입 연산은 항상 우변부터 처리
n1 = 10 + 5
print("n1 =", n1)

n1 = n1 + n2
print("n1 =", n1)

-------------------------------------------------------------------------------------------------

# format : 형태, 서식
# formating : 문자열에 형태만 잡고, 데이터를 뒤에서 채우는 방식

# 1. C언어 방식(= 옛날 방식)
name = "홍길동"
age = 25

print("이름 : %s (%d세)" % (name, age))

# 2. format 함수 방식(= 가장 보편적)
print("이름 : {} ({}세)\n".format(name, age))

# 3. f-string 방식(= 최신)
print(f"이름 : {name} ({age}세)")

-------------------------------------------------------------------------------------------------

# 1. C언어 방식
# % 라고 하는 포멧(=서식) 문자를 사용하는 방식

# %s : string, 문자열이 들어갈 자리
# %d : decimal, 정수가 들어갈 자리
# %f : float, 실수가 들어갈 자리

name = "홍길동"
age = 25
height = 175.5

print("이름은 %s입니다" %name) # 변수를 하나만 작성시 () 불필요

print("이름은 %s, 나이는 %d세 입니다" %(name, age))
# 변수를 둘 이상 작성시 괄호 필수
# 왼쪽부터, 순서대로 채워진다

print("이름은 %s, 나이는 %d세, 키는 %fcm 입니다" %(name, age, height))
# %f는 기본 소수점 6자리까지 무조건 출력

print("이름은 %s, 나이는 %d세, 키는 %.1fcm 입니다\n" %(name, age, height))
# %.nf 는 n자리만큼 소수점을 제어

# print("이름은 %f, 나이는 %s세, 키는 %dcm 입니다" % (name, age, height))
# 포멧 문자를 맞추지 않으면 에러~

# 포멧은 문자열로 저장 후 출력도 가능하다
reustl = "이름은 %s, 나이는 %d세 입니다" %(name, age)
print(result)

# 포멧 문자열을 변수에 저장하고, 나중에 채울 수도 있다
result = "이름은 %s, 나이는 %d세, 키는 %.1fcm 입니다"
print("result % (name, age, height))

-------------------------------------------------------------------------------------------------

# 2. format 
# 문자열 함수 format()을 사용한 방식
# 포멧울 모두 {}로 처리

name = "홍길동"
age = 25
height = 181.5

print("이름은 {}입니다".format(name))

print("이름은 {}, 나이는 {}세 입니다".format(name, age))

print("이름은 {}, 나이는 {}세, 키는 {}cm 입니다".format(name, age, height))
# format 방식은 소수점이 작성된 만큼 알아서 제어된다

print("이름은 {}, 나이는 {}세, 키는 {:2f}cm 입니다\n".format(name, age, height))
# 하지만, 나누기를 했을때 무한대로 수렴하면 잘라야한다~
# {:nf} 방식으로 제어 가능 (주의, 콜론(:)을 빼먹지 말자)

print("이름은 {}, 나이는 {}세, 키는 {}cm 입니다".format(name, age, height))
# format()의 {}에는 순서가 있다                         0     1     2

print("이름은 {2}, 나이는 {0}세, 키는 {1}cm 입니다".format(name, age, height))
# 번호를 작성하면, 원하는 위치에 원하는 값을 채울수 있다

print("이름은{1}, 나이는 {1}, 키는 {1}cm 입니다".format(name, age, hright))
# 콜론(:)은 순서번호와 기능(= 소수점 제어)의 구분자가 된다
# {순서번호 : 기능}

# print("이름은 {1}, 나이는 {1}, 키는 {1}cm 입니다".format(name))
# 없는 순서번호를 사용하면 에러~

# format()의 {}의 기능 부분을 살펴보자
# {순서번호 : 기능}

# 1. 소수점 제어
print("소수점 : {}".format(3.141592))
print("소수점 : {:.1f}\n".format(3.141592))

# ※ 순서 번호를 사용하지 않더라도, 기능과 순서번호 구분을 위해서 : 은 무조건 찍는다

# 2. 돈단위 제어
money = 1000000000

print("{}원".format(money))
print("{:,}원\n".format(money))

# 3. 출력 공간 제어
print("{}<-".format("Python"))

print("{:<10}<-".format("Python"))  # < : 좌측 정렬
print("{:>10}<-".format("Python"))  # > : 우측 정렬
print("{:^10}<-".format("Python"))  # ^ : 가운데 정렬
print()

print("{:#<20}".format("Python"))   # 정렬기호 앞에 문자를 작성하면
print("{:#>20}".format("Python"))   # 공백 대신 작성한 문자가 빈곳에 채워진다
print("{:#^20}".format("Python"))
print()

print("{:=^20} - {:#^20}".format("Hello", "Python"))
print("{1:=^20} - {0:#^20}".format("Hello", "Python"))

# 지금한 기능들은 당장에 너무 외우려고 할 필요 없다
# 가장 기본 사용방법인 {}만 잘써도 된다~

-------------------------------------------------------------------------------------------------

# f-string 방식
# format과 거의 유사하다
# 뒤에서 값을 채우는 것이 아니라, {} 안에 바로 변수나 값을 채운다.

name = "홍길동"
age = 25
height = 171.1

print(f"이름은 {name}, 나이는 {age}, 키는 {height}cm 입니다")
# 변수나 값을 바로 채우기 때문에, 순서번호가 없다

print(f"이름은 {name}, 나이는 {age}, 키는 {height:.2f}cm 입니다\n")
# 단, 기능은 똑같이 : 뒤에 작상하면 동작한다

money = 10000000

print(f"금액 : {money}원")
print(f"금액 : {money:,}원\n")

print(f"{'Python':=^30} - {'Hello':#^30}")

